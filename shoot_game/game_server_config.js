// Public deployment setting.
// After your WebSocket server is deployed, replace the empty string with its
// WSS address. Example: "wss://club-game-server.onrender.com"
// Keep this empty when deploying to a separate WebSocket host. Local testing
// uses the port below so it does not conflict with an older server on 8081.
window.GAME_SERVER_URL = "https://shooting-game-i5f4.onrender.com";
window.GAME_SERVER_PORT = 8082;
